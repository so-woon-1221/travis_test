language: node_js
node_js:
  - 14

branches:
  only:
    - main

services: docker

before_install:
  - echo "$DOCKER_PASSSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker build -t janus1221/next-test-2 -f Dockerfile .
  
notifications:
  email:
    recipients:
      - janus7359@gmail.com